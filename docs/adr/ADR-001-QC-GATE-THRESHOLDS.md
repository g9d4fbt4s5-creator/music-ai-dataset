# ADR-001: QC Gate 阈值决策记录

**状态**: Accepted  
**日期**: 2026-08-25  
**决策者**: 数据工程团队  
**影响范围**: scripts/00.5_cleaning/qc_gate.py, quality_check.py

---

## 背景

音乐数据集构建过程中，需要对采集的音频进行质量检查，分为 pass / marginal / fail 三分支。
fail 不进入下游 Stage，marginal 进入下游但标记 flag_for_review，pass 直接放行。

阈值的设置直接影响数据质量和人工审核工作量，需要基于数据分布和人工听检结果持续调整。

---

## 决策

### 1. 三分支合并逻辑

**规则**: 任一维度 fail → 最终 fail；否则任一维度 marginal → 最终 marginal；全 pass → pass。

**理由**: 避免单一指标误杀优质样本（如老爵士高 DR 但低 SNR），但任一维度严重不合格（如无法解码、非音乐）必须 fail。

### 2. 内容分类 (YAMNet)

| 阈值 | 值 | 理由 |
|------|-----|------|
| music_score_pass | 0.7 | YAMNet 置信度高，直接放行 |
| music_score_fail | 0.3 | YAMNet 置信度低，判为非音乐 |
| 中间区间 | 0.3~0.7 | marginal，人工确认 |

**重要约束**: YAMNet 阈值**不因为域外样本（Ace Studio 生成 + Demucs 分轨人声单轨）而调整**。
这类样本应在数据采集阶段通过 `source_type` 标记排除，而非修改模型阈值。

### 3. 音质指标 (librosa)

| 指标 | fail | marginal | pass / info | 理由 |
|------|------|----------|-------------|------|
| SNR (dB) | < 10 | 10 ~ 12 | ≥ 12 | 9首marginal人工听检100%可接受，从15dB放宽到12dB；老爵士/黑胶/风格乐器效果器正常底噪范围10-12dB |
| 削波比例 | > 5% | 2% ~ 5% | ≤ 2% | 大量削波带来严重失真；少量瞬间削波允许 |
| 静音比例 | > 80% | 50% ~ 80% | ≤ 50% | 长前奏/间奏静音正常；>80%无有效音乐内容 |
| DR (dB) | < 3 | 3 ~ 5 | ≥ 5；>20 仅 info | 高DR是优点（古典/爵士/原声常见），不是缺陷；低DR才是过度压缩 |

### 4. v6 新增指标

| 指标 | fail | marginal | pass | 理由 |
|------|------|----------|------|------|
| LUFS 集成响度 | < -36 或 > -6 | -36~-28 或 -11~-6 | -28~-11 | 音乐标准响度区间；太轻/爆响标记，极端直接fail |
| DC offset | > 0.15 | 0.05 ~ 0.15 | < 0.05 | 直流偏移带来爆音（预留接口，quality报告暂无此字段） |
| 可解码性 | corrupted=True | — | 正常 | 损坏音频直接fail |

### 5. 时长

| 阈值 | 值 | 理由 |
|------|-----|------|
| duration_min_fail | 5s | <5s 碎片无音乐信息 |
| duration_long_form | 15min | >15min 标记 long_form，marginal |
| duration_dj_mix | 30min | >30min 标记 dj_mix，需分段 |

**不采用**: >720s 直接 fail。理由：会误杀长即兴爵士/古典乐章。

### 6. 源质量

| 指标 | marginal | pass / info | 理由 |
|------|----------|-------------|------|
| 采样率 | < 22050 Hz | ≥ 22050 Hz | 低采样率影响下游特征提取 |
| 比特率 | orig_bitrate < 128kbps | ≥ 128kbps | 只检查原始有损格式；FLAC无损不检查 |
| 声道 | — | mono 仅 info | 很多爵士/老录音本身就是单声道，不是质量问题 |

---

## 阈值调整历史

| 日期 | 调整 | 原因 | 听检支撑 |
|------|------|------|---------|
| 2026-08-24 | SNR marginal 15→12dB | 9首marginal人工听检100%可接受（噪声为音乐制作正常特征：曲风/乐器/效果器/演奏法/历史录音） | 9首全检，7首SNR 13-15dB升级为pass |
| 2026-08-25 | DR >20 从 fail 改为 info | 高DR是优点，古典/爵士/原声常见；旧代码DR=184dB是计算bug | 27首中25首DR>20，全部为正常高动态录音 |
| 2026-08-25 | FLAC 比特率检查移除 | FLAC无损压缩比特率随内容动态变化，不反映源质量 | 代码修复：只检查 orig_bitrate |
| 2026-08-25 | mono 从 marginal 改为 info | 很多爵士/老录音本身就是单声道，不是质量问题 | 19首新采集jazz中多首mono，全部为正常录音 |

---

## 工程约束

1. **marginal 样本不自动删除，也不直接进训练集**：进入 marginal 候选池，人工 Label-Studio 复核后升级 pass 或标记 fail。
2. **老爵士乐大量黑胶转录、现场录音会落到 marginal**：不能脚本直接过滤，防止大量有效样本丢失。
3. **fail 样本不删除物理文件**：物理归档备份，仅做逻辑排除，防止误判。
4. **阈值调整必须有听检支撑**：不允许凭感觉调整阈值，必须基于人工听检结果和数据分布。
5. **阈值后抽检 (post_threshold_audit)**：阈值调整后，自动识别阈值漂移样本并分层抽检，劣质率>20%自动回滚。

---

## 岗位边界

- **数据岗**: 批量计算全套指标，执行 pass/marginal/fail 逻辑，输出 marginal 待审核清单，统计分布。
- **评测岗**: 使用 marginal 样本做鲁棒性测试，测试模型在噪声、失真样本的表现。

---

## 参考

- [EBU R128 响度标准](https://tech.ebu.ch/publications/r128)
- [YAMNet 论文](https://arxiv.org/abs/1706.08629)
- [AudioSet 数据集](https://research.google.com/audioset/)
